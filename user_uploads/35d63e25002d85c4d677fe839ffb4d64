#Creating binary in type str
binary = input("Enter binary number: \n")
#Initialising denary for later use
denary = 0
#"Reversing" the str binary for calculation
binary = binary[::-1]

#Creating for loop which runs for length of binary string
for sub in range(0, len(binary)):
        #Adding the subordinates value times 2^(subordinates value) to denary
        denary += (int(binary[sub]))*(2**sub)

#Print statement for final calculation
print ("Denary of binary given = " + str(denary))
        


